<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.a3130project">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".PrescriptionDetails"/>
        <activity android:name=".MedicationDetails" />
        <activity android:name=".AllMeds" />
        <activity android:name=".dosageActivity" />
        <activity android:name=".calendarActivity" />
        <activity android:name=".dosageActivity"/>
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".MainProfileLoadActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RefillActivity" />
        <activity android:name=".ScheduleActivity"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <receiver android:name=".AlarmBroadCastReceiver">
            <intent-filter>
                <action android:name="android.alarm.receiver" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
